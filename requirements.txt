# Runtime dependencies.
# Version ceilings keep the project installable on Python 3.9, which is the
# lowest interpreter the README advertises.
anthropic>=0.40,<1.0
openai>=1.0.0,<2.0
pypdf>=5.0,<6.0
streamlit>=1.38,<1.45      # 1.45+ drops Python 3.9 support
numpy>=1.26,<2.1           # evaluation simulation only
python-dotenv>=1.0,<2.0

# Removed in the audit: pandas, scikit-learn and matplotlib were declared here
# but never imported anywhere in src/ or tests/. They added ~200MB of install
# weight and three extra CVE surfaces for no functionality. Reinstate them in
# requirements-dev.txt if notebooks under notebooks/ start needing them.
